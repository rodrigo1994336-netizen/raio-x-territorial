name: V47 Curvelo Visual Smoke

on:
  pull_request:
    branches: [ main ]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  curvelo-visual:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    env:
      RX_RELEASE: V8_OPERATIONAL_ZERO_COST
      RX_PORTAL_RSS_BUDGET_MB: '220'
      RX_BIGQUERY_PROJECT: ${{ secrets.GCP_PROJECT_ID }}
      RX_ADMIN_MUNICIPALITY_TABLE: basedosdados.br_geobr_mapas.municipio
      RX_ADMIN_UF_TABLE: basedosdados.br_geobr_mapas.uf
      RX_BIGQUERY_MAX_BYTES_BILLED: '42949672960'
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Reject missing Google Cloud secret configuration
        shell: bash
        env:
          GCP_SA_KEY_PRESENT: ${{ secrets.GCP_SA_KEY }}
          GCP_PROJECT_ID_PRESENT: ${{ secrets.GCP_PROJECT_ID }}
        run: |
          test -n "$GCP_SA_KEY_PRESENT" || { echo 'GCP_SA_KEY ausente'; exit 2; }
          test -n "$GCP_PROJECT_ID_PRESENT" || { echo 'GCP_PROJECT_ID ausente'; exit 2; }
      - name: Authenticate BigQuery for real V47 panel
        uses: google-github-actions/auth@v2
        with:
          credentials_json: ${{ secrets.GCP_SA_KEY }}
          project_id: ${{ secrets.GCP_PROJECT_ID }}
          create_credentials_file: true
          export_environment_variables: true
      - name: Install runtime and Chromium
        run: |
          python -m pip install --upgrade pip
          pip install fastapi httpx shapely pyproj pillow reportlab uvicorn "psycopg[binary]" "redis>=5,<7" "numpy>=2,<3" "rasterio>=1.4,<2" "PyJWT[crypto]>=2.10,<3" playwright google-cloud-bigquery google-auth
          python -m playwright install --with-deps chromium
      - name: Start authenticated V47 runtime
        run: |
          set -euo pipefail
          mkdir -p artifacts-v47
          export PYTHONPATH="$PWD${PYTHONPATH:+:$PYTHONPATH}"
          uvicorn portal_api:app --host 127.0.0.1 --port 8000 > /tmp/rx-v47.log 2>&1 &
          echo $! > /tmp/rx-v47.pid
          ready=0
          for i in $(seq 1 90); do
            if ! kill -0 "$(cat /tmp/rx-v47.pid)" 2>/dev/null; then
              echo "runtime exited" > artifacts-v47/runtime-start-failure.txt
              tail -n 300 /tmp/rx-v47.log || true
              exit 1
            fi
            html="$(curl -fsS http://127.0.0.1:8000/ 2>/dev/null || true)"
            if [[ "$html" == *"RX_CAR_INTEGRITY_V47"* && "$html" == *"RX_MAP_V46_TWO_LEVEL"* ]]; then
              ready=1
              echo "RX_V47_VISUAL_RUNTIME=READY attempt=$i"
              break
            fi
            sleep 1
          done
          test "$ready" -eq 1
      - name: Capture Curvelo 375 768 1440 with real BigQuery data
        run: python .github/scripts/v47_curvelo_visual_smoke.py
      - name: Runtime log on failure
        if: failure()
        run: tail -n 400 /tmp/rx-v47.log || true
      - name: Upload V47 Curvelo captures
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: v47-curvelo-visual-${{ github.run_id }}
          path: artifacts-v47/
          if-no-files-found: error
          retention-days: 14
