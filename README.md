# Raio-X Territorial

Backend geoespacial do Raio-X Territorial.

Checkpoint implantável: V0.14.

O deploy no Render usa `project.tar.gz` para preservar exatamente o conjunto de arquivos validado no checkpoint local.
